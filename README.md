# neo-net

A head-only library for Neural Networks Frame. Inspired by Tsoding.

## Quick Start

Before using it, make sure you have configured the raylib library.

```bash
$ ./build.sh
$ ./img2nn ./mnist/train/8/train_image_10057.png ./mnist/train/6/train_image_10032.png
```
